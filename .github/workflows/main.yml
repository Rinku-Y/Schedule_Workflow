name: schedule workflow

on:
  schedule:
    - cron: "7 22 * * *"

jobs:
  run-schedule:
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "schedule workflow executed"
